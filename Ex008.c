/* OUTRA TENTATIVA DO EX009.C

#include <stdio.h>
#include <stdlib.h>

int main() {
    float item1 = 4.00, item2 = 4.50, item3 = 5.00, item4 = 2.00, item5 = 1.50, totPreco;
    int cod, qtd;
    printf("Digite um codigo e a quantidade: ");
    scanf("%d", &cod);
    printf("Qtd");
    scanf("%d", &qtd);

    if (cod = 1) {
        totPreco = (item1 * qtd);
        printf("Total a ser pago: %f", totPreco);
    } 
return 0;
}

/*
else { if (cod = 2) {
        totPreco = (item2 * qtd);
        printf("Total a ser pago: %g", totPreco);
    } else { if (cod = 3) {
        totPreco = (item3 * qtd);
        printf("Total a ser pago: %g", totPreco);
    } else { if (cod = 4) {
        totPreco = (item4*qtd);
        printf("Total a ser pago: %f", totPreco);
    } else { if (cod = 5) {
        totPreco = (item5*qtd);
        printf("Total a ser pago: %g", totPreco); 
    } else { printf("Codigo invalido. Tente novamente");
    }}
    }}
    }
    
*/