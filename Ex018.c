//

#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i, n, soma;

    printf("Informe n: ");
    scanf("%i", &n);

    for(i = 0; i < n; ++i)
    {
        n = n + i;
        printf("%i \n", soma);
    }

    return 0;
}
